{
  "name": "grid",
  "version": "1.0.0",
  "description": "Grid energy source component",
  "dependencies": ["energy_base"],
  "config_schema": {
    "max_import_power": {
      "type": "number",
      "description": "Maximum import power in watts",
      "required": true
    },
    "max_export_power": {
      "type": "number",
      "description": "Maximum export power in watts",
      "required": true
    }
  }
}